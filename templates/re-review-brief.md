# Бриф повторного ревью: [название задачи]

## Target

[Компонент и поведение, проверяемые только в пределах дельты.]

## Базовый SHA дифа

`[base_sha]`

Проверь diff `[base_sha]..[current_sha]`, а не весь проект и не исходный change-set.

## Конкретные находки первого ревью

1. [ID] — [что было неверно и критерий закрытия].
2. [ID] — [что было неверно и критерий закрытия].

## Scope

[Область, зафиксированная первым ревью. Новые темы за её пределами перечисли отдельно как новые задачи, не расширяя анализ.]

## Acceptance

[Для каждой находки нужен вердикт PASS/FAIL и факт из дельты, который его подтверждает.]

## Отчёт

Первая строка: `DONE` при PASS всех перечисленных находок, иначе `DONE_WITH_CONCERNS` или `BLOCKED` с причиной.
Далее: `[ID] — PASS/FAIL — доказательство из diff`; затем отдельный список новых находок вне scope без расширенного анализа. Заверши `worker_done` с подходящим `--outcome succeeded|failed`.
